node('dotnet-31') {

  stage('Clone') {
    checkout scm
  }

  stage('Test') {
	dir('app') {
	  script {
		def props = readProperties file:'build.properties'
		version = props['version']
		echo "version: ${version}"
	  }
	}
  }

  stage('Run') {
    sh "dotnet --version"
  }


  
}
